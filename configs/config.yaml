env: "local"

http_server:
   port: "8080"
   adress: "0.0.0.0"

logger:
   dir: "./logs"

redis:
   host: "localhost"
   port: "6379"
   password: ""
   db: 0
   ttl_seconds: 120

postgress:
   host: "localhost"
   port: "5432"
   user: "postgress"
   db_name: "finance_db"
   db_type: "postgres"
